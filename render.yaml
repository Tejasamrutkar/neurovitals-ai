services:
  - type: web
    name: neurovitals-ai
    env: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: python backend/run_server.py
    envVars:
      - key: PORT
        value: 8000